


document.addEventListener("DOMContentLoaded", function() {

    const templateSource = document.querySelector("#days_template").innerHTML

    const daysTemplate = Handlebars.compile(templateSource)


})